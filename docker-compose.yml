version: '3.6'
services:
        engmaster_bot:
                build: .
                restart: always

        postgres:
                image: postgres
                restart: always
                environment:
                        - POSTGRES_PASSWORD=cabeiMoh4Pah